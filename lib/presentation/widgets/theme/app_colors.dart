import 'package:flutter/material.dart';

class AppColors {
  // Common Colors
  static const primaryColor = Color(0xFFCD1818);
  static const accentColor = Color(0xFFF95454);

  // Light Theme Colors
  static const lightBackground = Color(0xFFFFE4D6);
  static const lightPrimaryText = Color(0xFF141E46);
  static const lightPrimaryCard = Color(0xFFFFF5E0);

  // Dark Theme Colors
  static const darkBackground = Color(0xFF222831);
  static const darkPrimaryCard = Color(0xFF31363F);
  static const darkPrimaryText = Color(0xFFEEEDEB);
}
